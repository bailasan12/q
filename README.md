## Render

للحفاظ على المنتجات والصور بعد إعادة تشغيل الخدمة:

1. أضف Persistent Disk في Render.
2. اجعل Mount Path هو `/var/data`.
3. أضف متغير البيئة `DATA_DIR` بقيمة `/var/data`.
4. أعد نشر الخدمة.

بدون Persistent Disk ستُفقد قاعدة البيانات والملفات المرفوعة عند إعادة إنشاء الخدمة.
